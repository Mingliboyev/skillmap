export function Progress({value,label}:{value:number;label?:string}) { const safe=Math.max(0,Math.min(100,value));return <div className="space-y-2"><div className="flex justify-between gap-3 text-sm text-secondary-text">{label && <span>{label}</span>}<span className="font-semibold text-primary-text">{value}%</span></div><div className="print-progress h-2.5 overflow-hidden rounded-full bg-slate-200" role="progressbar" aria-valuemin={0} aria-valuemax={100} aria-valuenow={safe} aria-label={label??"0–100"}><div className="h-full rounded-full bg-teal-700 transition-all" style={{width:`${safe}%`}}/></div></div> }
