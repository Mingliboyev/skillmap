"use client";import type{ReactNode}from "react";import {useState}from "react";
export function Tabs({items}:{items:{label:string;content:ReactNode}[]}){const[a,setA]=useState(0);return <div><div role="tablist" className="flex max-w-full gap-1 overflow-x-auto border-b border-ui-border">{items.map((x,i)=><button role="tab" aria-selected={a===i} key={x.label} onClick={()=>setA(i)} className={`shrink-0 border-b-2 px-4 py-3 text-sm font-bold focus-visible:outline-none ${a===i?"border-teal-800 bg-teal-50 text-teal-900":"border-transparent text-secondary-text hover:bg-slate-100 hover:text-primary-text"}`}>{x.label}</button>)}</div><div role="tabpanel" className="py-5 text-primary-text">{items[a]?.content}</div></div>}
