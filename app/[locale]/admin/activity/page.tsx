import { notFound } from "next/navigation";
import { getActivityLog } from "@/lib/admin-data";
import { isLocale } from "@/lib/i18n/dictionaries";
export default async function ActivityPage({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const rows=await getActivityLog(),uz=locale==="uz";return <div className="mx-auto max-w-[1400px]"><p className="text-xs font-bold uppercase tracking-wider text-slate-500">{uz?"Audit izi":"Audit trail"}</p><h1 className="mt-1 text-2xl font-bold">{uz?"Faoliyat jurnali":"Activity log"}</h1><p className="mt-1 text-sm text-slate-600">{uz?"So‘nggi 100 ta administrator hodisasi.":"The 100 most recent admin events."}</p><section className="mt-5 overflow-hidden rounded-lg border border-slate-300 bg-white">{rows.length?<div className="overflow-x-auto"><table className="w-full min-w-[900px] text-left text-xs"><thead className="bg-slate-50 text-slate-600"><tr><th className="px-3 py-3">{uz?"Vaqt":"Timestamp"}</th><th>{uz?"Ijrochi":"Actor"}</th><th>{uz?"Harakat":"Action"}</th><th>{uz?"Nishon":"Target"}</th><th>{uz?"Xulosa":"Summary"}</th></tr></thead><tbody className="divide-y divide-slate-200">{rows.map(row=><tr key={row.id} className="align-top"><td className="px-3 py-3">{new Date(row.created_at).toLocaleString()}</td><td className="font-mono">{row.actor_id?.slice(0,8)??"system"}</td><td className="font-semibold">{row.action}</td><td>{row.entity_type}<span className="block font-mono text-slate-500">{row.entity_id??"—"}</span></td><td><code className="block max-w-lg whitespace-pre-wrap break-all text-[11px] text-slate-600">{JSON.stringify(row.metadata)}</code></td></tr>)}</tbody></table></div>:<p className="p-8 text-center text-sm text-slate-600">{uz?"Faoliyat yozuvlari hali yo‘q.":"No activity records yet."}</p>}</section></div>}
