import Link from "next/link";import {notFound} from "next/navigation";import {AuthForm} from "@/features/auth/auth-form";import {isLocale} from "@/lib/i18n/dictionaries";
export default async function Page({params}:{params:Promise<{locale:string}>}){const {locale}=await params;if(!isLocale(locale))notFound();const uz=locale==="uz";return <div className="container-shell grid min-h-[700px] place-items-center py-16"><div className="w-full max-w-md rounded-2xl border border-slate-200 bg-white p-7 shadow-xl shadow-slate-200/50"><p className="text-xs font-bold uppercase tracking-widest text-teal-700">SkillMap Uzbekistan</p><h1 className="display mt-3 text-4xl font-semibold">{uz?"Xush kelibsiz":"Welcome back"}</h1><p className="mt-2 text-sm text-slate-500">{uz?"Natijalaringiz va yo‘l xaritangizni saqlang.":"Save your results and learning roadmap."}</p><div className="mt-8"><AuthForm locale={locale} mode="sign-in"/></div><p className="mt-6 text-center text-sm text-slate-500">{uz?"Hisobingiz yo‘qmi?":"New to SkillMap?"} <Link className="font-bold text-teal-700" href={`/${locale}/sign-up`}>{uz?"Ro‘yxatdan o‘ting":"Create account"}</Link></p></div></div>}
