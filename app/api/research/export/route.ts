import{getResearchSnapshot}from "@/lib/research";import{UTF8_BOM,toCsv}from "@/lib/csv";
export async function GET(){const data=await getResearchSnapshot();const rows=data.regionalScores.map(x=>({data_mode:data.mode,region:x.region,average_score:x.score,participants:x.participants,minimum_group_size:data.minimumGroupSize}));const columns=["data_mode","region","average_score","participants","minimum_group_size"]as const;const csv=UTF8_BOM+toCsv(rows,[...columns]);return new Response(csv,{headers:{"content-type":"text/csv; charset=utf-8","content-disposition":"attachment; filename=skillmap-regional-summary.csv","cache-control":"no-store"}})}
